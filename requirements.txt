# Deployment manifest for the webhook service (Docker and Vercel both use
# this). For local dev with the full CLI/training toolchain, prefer:
#   pip install -e ".[service,dev]"
# (see pyproject.toml). For an exact pinned snapshot of the environment that
# produced the shipped model artifacts (adds matplotlib/pytest, used only
# for reproducing the research), see requirements-freeze.txt.
#
# On Vercel this file is auto-detected and installed by Vercel's own
# optimizing installer (no custom installCommand — a custom install command
# disables Vercel's automatic function-bundle size optimization, which is
# the difference between fitting under the ~225MB effective limit and not).
# pyproject.toml is hidden from Vercel via .vercelignore so this file wins
# detection instead of pyproject.toml's base deps (which deliberately
# exclude these service/postgres extras to keep a plain CLI install light).
#
# Versions pinned to match requirements-freeze.txt — the shipped .joblib
# models were pickled under these scikit-learn/numpy/pandas versions.

pyyaml==6.0.2
python-dotenv==1.0.1
requests==2.32.3
pandas==3.0.3
scikit-learn==1.8.0
numpy==2.4.5
joblib==1.5.3
fastapi==0.139.0
PyJWT==2.13.0
cryptography==49.0.0
httpx==0.28.1
pg8000>=1.30
